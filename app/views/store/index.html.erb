<h3>Categories</h3>
<% @categories.each do |category| %>
  <%= link_to category.title, category_path(category) %>
<% end %>

<br><h3>Items</h3>
<% @items.each do |item| %>
  <p><%= render partial: 'layouts/merchandise', locals: {item: item} %></p>
<% end %>
